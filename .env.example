# Local: file:./dev.db (relativo a prisma/). Vercel MVP: mesmo valor; seed roda no build.
# Produção real: Postgres (ex. postgresql://...) e trocar provider no schema Prisma.
DATABASE_URL="file:./dev.db"
AUTH_SECRET="meu-plano-dev-secret-change-in-production"
AUTH_URL="http://localhost:43123"
NEXTAUTH_URL="http://localhost:43123"
AUTH_TRUST_HOST=true
